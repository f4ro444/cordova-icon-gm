#!/usr/bin/env node

var argv = require('yargs').argv;
var ionic = argv.ionic;
//console.log(ionic);
// print process.argv
//process.argv.forEach(function (val, index, array) {
//  console.log(index + ': ' + val);
//});

require('./../index.js').generate(ionic);
